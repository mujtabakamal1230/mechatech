const path = require('path');

// This file is used by GoDaddy's Node.js manager to start the server
// It points to the standalone server created by 'next build'
require('./.next/standalone/server.js');
